**ToolbarBadge** — the small green pill drawn over the browser-action icon after a context-menu copy. `✓` on success, `!` on failure; auto-clears after ~1.2s in the extension.

```jsx
<ToolbarBadge text="✓" />
<ToolbarBadge text="!" />
```

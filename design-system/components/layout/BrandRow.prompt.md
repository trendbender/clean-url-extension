**BrandRow** — the popup header: 34px app icon, title (16/700), and muted subtitle. Pass `logoSrc` for the PNG icon, or omit it to render the gradient "CU" monogram fallback.

```jsx
<BrandRow title="Clean URL" subtitle="One-click link cleaner" logoSrc="assets/logo-cu.svg" />
```
